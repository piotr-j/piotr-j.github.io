box: golang:1.5.1
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
        theme: "ghostwriter-clean"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - uetchy/gh-pages@0.3.1:
        token: $GIT_TOKEN
        domain: piotrjastrzebski.io
        path: public/
        